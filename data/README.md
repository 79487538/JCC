# Data Directory

This directory is reserved for deployment-level data files.

Current runtime rule libraries are stored in `backend/data/` because the FastAPI
backend loads them relative to `backend/main.py`.
